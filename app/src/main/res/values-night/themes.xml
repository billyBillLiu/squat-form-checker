<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <color name="opposite_button_color">#000000</color>
    <style name="Base.Theme.SquatDepthChecker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/custom_background_dark_gray</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@color/custom_background_dark_gray</item>
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorButtonNormal">@color/white</item>
    </style>
</resources>